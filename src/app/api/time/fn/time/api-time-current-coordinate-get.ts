/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CurrentTime } from '../../models/current-time';

export interface ApiTimeCurrentCoordinateGet$Params {

/**
 * Latitude ranging from -90 to 90
 */
  latitude?: number;

/**
 * Longitude ranging from -180 to 180
 */
  longitude?: number;
}

export function apiTimeCurrentCoordinateGet(http: HttpClient, rootUrl: string, params?: ApiTimeCurrentCoordinateGet$Params, context?: HttpContext): Observable<StrictHttpResponse<CurrentTime>> {
  const rb = new RequestBuilder(rootUrl, apiTimeCurrentCoordinateGet.PATH, 'get');
  if (params) {
    rb.query('latitude', params.latitude, {});
    rb.query('longitude', params.longitude, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<CurrentTime>;
    })
  );
}

apiTimeCurrentCoordinateGet.PATH = '/api/time/current/coordinate';
